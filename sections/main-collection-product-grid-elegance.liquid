{{ 'template-collection.css' | asset_url | stylesheet_tag }}
{{ 'component-loading-spinner.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}
{{ 'component-slider.css' | asset_url | stylesheet_tag }}

{% if section.settings.image_shape == 'blob' %}
  {{ 'mask-blobs.css' | asset_url | stylesheet_tag }}
{%- endif -%}

{%- if section.settings.enable_quick_add -%}
  {{ 'quick-add.css' | asset_url | stylesheet_tag }}
  <script src="{{ 'quick-add.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{%- style -%}
  .section-{{ section.id }}-padding {
  padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
  padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }
  .cpb-brand-page .cpb-contents-nav a,
  .cpb-brand-page .sp-only a {
    color: #000;
  }
  .cpb-brand-page .cpb-contents-nav a:hover,
  .cpb-brand-page .sp-only a:hover {
    color: #000;
  }
  .elegance-products-board {
    max-width: 1200px;
    margin: 0 auto;
  }
  .elegance-products-title {
    color: #b3a06a;
    font-size: 2.175rem;
    letter-spacing: 0.12em;
    font-weight: 500;
  }
  .elegance-group-title {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    margin-bottom: 1rem;
    color: #b3a06a;
    letter-spacing: 0.1em;
    font-weight: 500;
  }
  .elegance-group-title::before,
  .elegance-group-title::after {
    content: '';
    height: 1px;
    background: #dcd8cc;
    flex: 1;
  }
  .elegance-item-image {
    display: block;
    width: 100%;
    max-height: 88px;
    object-fit: contain;
  }
  .elegance-item-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }
  .elegance-item-label {
    margin: 0;
    font-size: 0.8rem;
    letter-spacing: 0.06em;
    color: #666;
    text-align: center;
  }
  .brand-new-items {
    width: 100%;
    margin: 0 auto;
  }
  .brand-new-items__grid {
    width: min(100%, 1200px);
    margin: 0 auto;
  }
  .brand-new-items__grid--compact {
    width: min(100%, calc(var(--brand-new-items-desktop-cols, 4) * 14rem + (var(--brand-new-items-desktop-cols, 4) - 1) * 1.5rem));
  }
  .brand-new-items__actions {
    margin-top: 2rem;
    text-align: center;
  }
  .brand-new-items__link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 12rem;
    min-height: 3rem;
    padding: 0.75rem 1.75rem;
    border: 1px solid #b3a06a;
    color: #b3a06a;
    text-decoration: none;
    letter-spacing: 0.08em;
  }
  .brand-new-items__link:hover {
    background: #b3a06a;
    color: #fff;
  }
  .brand-counseling-banner {
    margin: 0.75rem auto;
    display: flex;
    justify-content: center;
  }
  .brand-counseling-banner__link {
    display: block;
    width: 50%;
  }
  .brand-counseling-banner__image {
    display: block;
    width: 100%;
    height: auto;
  }
  @media screen and (max-width: 749px) {
    .brand-counseling-banner {
      width: 100%;
    }
    .brand-counseling-banner__link {
      width: 100%;
    }
  }
  @media screen and (min-width: 750px) {
  .section-{{ section.id }}-padding {
  padding-top: {{ section.settings.padding_top }}px;
  padding-bottom: {{ section.settings.padding_bottom }}px;
  }
  }
{%- endstyle -%}

{%- liquid
  assign products_to_display = section.settings.products_to_show

  assign show_mobile_slider = false
  assign show_mobile_slider = true

  assign show_desktop_slider = false
  assign show_desktop_slider = true

  # Determine selected tag from Storefront Filtering (filter.p.tag) または旧来の current_tags
  assign filter_tag = ''
  for filter in collection.filters
    if filter.param_name == 'filter.p.tag'
      if filter.active_values != blank
        assign filter_tag = filter.active_values.first.label
      endif
      break
    endif
  endfor
  if filter_tag == ''
    assign filter_tag = current_tags | first
  endif

  assign is_new_items_template = false
  if template.suffix contains 'new-items'
    assign is_new_items_template = true
  endif

  assign new_arrival_count = 0
  if section.settings.new_arrival_collection != blank
    assign new_arrival_count = section.settings.new_arrival_collection.products_count
  endif

  assign show_new_arrival_slider = false
  if new_arrival_count > section.settings.products_to_show
    assign show_new_arrival_slider = true
  endif

  assign show_new_arrival_view_all = false
  if new_arrival_count >= 5 and section.settings.new_arrival_collection != blank
    assign show_new_arrival_view_all = true
  endif

  assign new_arrival_mobile_cols = section.settings.columns_mobile | plus: 0
  if new_arrival_count > 0 and new_arrival_count < new_arrival_mobile_cols
    assign new_arrival_mobile_cols = new_arrival_count
  endif
  if new_arrival_mobile_cols == 0
    assign new_arrival_mobile_cols = 1
  endif

  assign new_arrival_desktop_cols = section.settings.columns_desktop
  if new_arrival_count > 0 and new_arrival_count < new_arrival_desktop_cols
    assign new_arrival_desktop_cols = new_arrival_count
  endif
  if new_arrival_desktop_cols == 0
    assign new_arrival_desktop_cols = 1
  endif

  assign new_arrival_products_to_display = section.settings.products_to_show
  if new_arrival_count > 0 and new_arrival_count < new_arrival_products_to_display
    assign new_arrival_products_to_display = new_arrival_count
  endif

  assign ranking_count = 0
  if section.settings.ranking_collection != blank
    assign ranking_count = section.settings.ranking_collection.products_count
  endif

  assign show_ranking_slider = false
  if ranking_count > section.settings.products_to_show
    assign show_ranking_slider = true
  endif

  assign ranking_mobile_cols = section.settings.columns_mobile | plus: 0
  if ranking_count > 0 and ranking_count < ranking_mobile_cols
    assign ranking_mobile_cols = ranking_count
  endif
  if ranking_mobile_cols == 0
    assign ranking_mobile_cols = 1
  endif

  assign ranking_desktop_cols = section.settings.columns_desktop
  if ranking_count > 0 and ranking_count < ranking_desktop_cols
    assign ranking_desktop_cols = ranking_count
  endif
  if ranking_desktop_cols == 0
    assign ranking_desktop_cols = 1
  endif

  assign ranking_products_to_display = section.settings.products_to_show
  if ranking_count > 0 and ranking_count < ranking_products_to_display
    assign ranking_products_to_display = ranking_count
  endif
-%}

{% comment %}
  filter.p.tag パラメータが Shopify の Storefront Filter API で認識されない場合、
  旧形式の URL (/collections/elegance/tag-name) にリダイレクトする
{% endcomment %}
{% unless filter_tag %}
  <script>
    (function() {
      var params = new URLSearchParams(window.location.search);
      var tagParam = params.get('filter.p.tag');
      if (tagParam) {
        var newUrl = window.location.pathname + '/' + encodeURIComponent(tagParam);
        var viewParam = params.get('view');
        if (viewParam) {
          newUrl += '?view=' + encodeURIComponent(viewParam);
        }

        if (window.location.hash) {
          newUrl += window.location.hash;
        }

        window.location.replace(newUrl);
      }
    })();
  </script>
{% endunless %}

<!-- Elegance -->
<div class="item-list cpb-brand-page">
  {% if filter_tag or is_new_items_template %}
    <!-- Elegance商品一覧ここから -->
    <div class="page-width tw-my-5">
      {% if filter_tag %}
        <h3 class="tw-text-2xl tw-font-bold tw-my-2">{{ filter_tag }}</h3>
      {% elsif is_new_items_template %}
        <h3 class="tw-text-center tw-text-3xl tw-mb-8 elegance-products-title">New Items & Colors</h3>
      {% endif %}
    </div>
    <div id="Collection" class="section-{{ section.id }}-padding">
      {%- paginate collection.products by section.settings.products_per_page -%}
        {% comment %} Sort is the first tabbable element when filter type is vertical {% endcomment %}
        {%- if section.settings.enable_sorting and section.settings.filter_type == 'vertical' -%}
          <facet-filters-form class="facets facets-vertical-sort page-width small-hide no-js-hidden">
            <form class="facets-vertical-form" id="FacetSortForm">
              <div class="facet-filters sorting caption">
                <div class="facet-filters__field">
                  <h2 class="facet-filters__label caption-large text-body">
                    <label for="SortBy">{{ 'products.facets.sort_by_label' | t }}</label>
                  </h2>
                  <div class="select">
                    {%- assign sort_by = collection.sort_by | default: collection.default_sort_by -%}
                    <select
                      name="sort_by"
                      class="facet-filters__sort select__select caption-large"
                      id="SortBy"
                      aria-describedby="a11y-refresh-page-message"
                    >
                      {%- for option in collection.sort_options -%}
                        <option
                          value="{{ option.value | escape }}"
                          {% if option.value == sort_by %}
                            selected="selected"
                          {% endif %}
                        >
                          {{ option.name | escape }}
                        </option>
                      {%- endfor -%}
                    </select>
                    {% render 'icon-caret' %}
                  </div>
                </div>
                <noscript>
                  <button type="submit" class="facets__button-no-js button button--secondary">
                    {{ 'products.facets.sort_button' | t }}
                  </button>
                </noscript>
              </div>

              <div class="product-count-vertical light" role="status">
                <h2 class="product-count__text text-body">
                  <span id="ProductCountDesktop">
                    {%- if collection.results_count -%}
                      {{
                        'templates.search.results_with_count'
                        | t: terms: collection.terms, count: collection.results_count
                      }}
                    {%- elsif collection.products_count == collection.all_products_count -%}
                      {{ 'products.facets.product_count_simple' | t: count: collection.products_count }}
                    {%- else -%}
                      {{
                        'products.facets.product_count'
                        | t: product_count: collection.products_count, count: collection.all_products_count
                      }}
                    {%- endif -%}
                  </span>
                </h2>
                <div class="loading__spinner">
                  <svg
                    aria-hidden="true"
                    focusable="false"
                    class="spinner"
                    viewBox="0 0 66 66"
                    xmlns="http://www.w3.org/2000/svg"
                  >
                    <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                  </svg>
                </div>
              </div>
            </form>
          </facet-filters-form>
        {%- endif -%}

        <div class="{% if section.settings.filter_type == 'vertical' %} facets-vertical page-width{% endif %}">
          {{ 'component-facets.css' | asset_url | stylesheet_tag }}
          <script src="{{ 'facets.js' | asset_url }}" defer="defer"></script>
          {%- if section.settings.enable_filtering or section.settings.enable_sorting -%}
            <aside
              aria-labelledby="verticalTitle"
              class="facets-wrapper{% unless section.settings.enable_filtering %} facets-wrapper--no-filters{% endunless %}{% if section.settings.filter_type != 'vertical' %} page-width{% endif %}"
              id="main-collection-filters"
              data-id="{{ section.id }}"
            >
              {% render 'facets',
                results: collection,
                enable_filtering: section.settings.enable_filtering,
                enable_sorting: section.settings.enable_sorting,
                filter_type: section.settings.filter_type,
                paginate: paginate
              %}
            </aside>
          {%- endif -%}

          <div
            class="product-grid-container{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
            id="ProductGridContainer"
            {% if settings.animations_reveal_on_scroll %}
              data-cascade
            {% endif %}
          >
            {%- if collection.products.size == 0 -%}
              <div class="collection collection--empty page-width" id="product-grid" data-id="{{ section.id }}">
                <div class="loading-overlay gradient"></div>
                <div class="title-wrapper center">
                  <h2 class="title title--primary">
                    {{ 'sections.collection_template.empty' | t -}}
                    <br>
                    {{
                      'sections.collection_template.use_fewer_filters_html'
                      | t: link: collection.url, class: 'underlined-link link'
                    }}
                  </h2>
                </div>
              </div>
            {%- else -%}
              <div class="collection{% if section.settings.filter_type != 'vertical' %} page-width{% endif %}">
                <div class="loading-overlay gradient"></div>

                <ul
                  id="product-grid"
                  data-id="{{ section.id }}"
                  class="
                    grid product-grid grid--{{ section.settings.columns_mobile }}-col-tablet-down
                    grid--{{ section.settings.columns_desktop }}-col-desktop
                  "
                >
                  {%- for product in collection.products -%}
                    {% assign lazy_load = false %}
                    {%- if forloop.index > 2 -%}
                      {%- assign lazy_load = true -%}
                    {%- endif -%}
                    <li
                      class="grid__item{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
                      {% if settings.animations_reveal_on_scroll %}
                        data-cascade
                        style="--animation-order: {{ forloop.index }};"
                      {% endif %}
                    >
                      {% render 'card-product',
                        card_product: product,
                        media_aspect_ratio: section.settings.image_ratio,
                        image_shape: section.settings.image_shape,
                        show_secondary_image: section.settings.show_secondary_image,
                        show_vendor: section.settings.show_vendor,
                        show_rating: section.settings.show_rating,
                        lazy_load: lazy_load,
                        show_quick_add: section.settings.enable_quick_add,
                        max_title_length: section.settings.max_title_length,
                        section_id: section.id
                      %}
                    </li>
                  {%- endfor -%}
                </ul>

                {%- if paginate.pages > 1 -%}
                  {% render 'pagination', paginate: paginate, anchor: '' %}
                {%- endif -%}
              </div>
            {%- endif -%}
          </div>
        </div>
      {%- endpaginate -%}
      {% if section.settings.image_shape == 'arch' %}
        {% render 'mask-arch' %}
      {%- endif -%}

    </div>
    <!-- Elegance商品一覧ここまで -->
  {% else %}
    <!-- Eleganceブランドトップ -->
    <div class="page-width">
      {%- if section.settings.new_arrival_collection != blank -%}
      <div class="cpb-items tw-my-10">
        <h3 class="tw-text-center tw-text-3xl tw-mb-8 elegance-products-title">New Items & Colors</h3>
        <slider-component class="brand-new-items slider-mobile-gutter{% if section.settings.full_width %} slider-component-full-width{% endif %}{% if show_new_arrival_slider == false %} page-width{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
          <ul
            id="Slider-{{ section.id }}-new-arrival"
            class="brand-new-items__grid{% unless show_new_arrival_slider %} brand-new-items__grid--compact{% endunless %} grid product-grid contains-card contains-card--product{% if settings.card_style == 'standard' %} contains-card--standard{% endif %} grid--{{ new_arrival_desktop_cols }}-col-desktop{% if section.settings.new_arrival_collection == blank %} grid--2-col-tablet-down{% else %} grid--{{ new_arrival_mobile_cols }}-col-tablet-down{% endif %}{% if show_new_arrival_slider %} slider slider--tablet grid--peek{% endif %}"
            role="list"
            aria-label="{{ 'general.slider.name' | t }}"
            style="--brand-new-items-desktop-cols: {{ new_arrival_desktop_cols }};"
          >
            {%- for product in section.settings.new_arrival_collection.products limit: section.settings.products_to_show -%}
              <li
                id="Slide-{{ section.id }}-new-arrival-{{ forloop.index }}"
                class="grid__item{% if show_new_arrival_slider %} slider__slide{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
                {% if settings.animations_reveal_on_scroll %}
                  data-cascade
                  style="--animation-order: {{ forloop.index }};"
                {% endif %}
              >
                {% render 'card-product',
                  card_product: product,
                  media_aspect_ratio: section.settings.image_ratio,
                  image_shape: section.settings.image_shape,
                  show_secondary_image: section.settings.show_secondary_image,
                  show_vendor: section.settings.show_vendor,
                  show_rating: section.settings.show_rating,
                  show_quick_add: section.settings.enable_quick_add,
                  max_title_length: section.settings.max_title_length,
                  section_id: section.id
                %}
              </li>
            {%- endfor -%}
          </ul>
          {%- if show_new_arrival_slider -%}
            <div class="slider-buttons no-js-hidden">
              <button
                type="button"
                class="slider-button slider-button--prev"
                name="previous"
                aria-label="{{ 'general.slider.previous_slide' | t }}"
                aria-controls="Slider-{{ section.id }}-new-arrival"
              >
                {% render 'icon-caret' %}
              </button>
              <div class="slider-counter caption">
                <span class="slider-counter--current">1</span>
                <span aria-hidden="true"> / </span>
                <span class="visually-hidden">{{ 'general.slider.of' | t }}</span>
                <span class="slider-counter--total">{{ new_arrival_products_to_display }}</span>
              </div>
              <button
                type="button"
                class="slider-button slider-button--next"
                name="next"
                aria-label="{{ 'general.slider.next_slide' | t }}"
                aria-controls="Slider-{{ section.id }}-new-arrival"
              >
                {% render 'icon-caret' %}
              </button>
            </div>
          {%- endif -%}
        </slider-component>
        {% if show_new_arrival_view_all %}
          <div class="brand-new-items__actions">
            <a class="brand-new-items__link" href="{{ section.settings.new_arrival_collection.url }}">一覧をみる</a>
          </div>
        {% endif %}
      </div>
      {%- endif -%}
      {%- if section.settings.ranking_collection != blank -%}
      <div class="cpb-items tw-my-10">
        <h3 class="tw-text-center tw-text-3xl tw-mb-8 elegance-products-title">RANKING</h3>
        <slider-component class="brand-new-items slider-mobile-gutter{% if section.settings.full_width %} slider-component-full-width{% endif %}{% if show_ranking_slider == false %} page-width{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
          <ul
            id="Slider-{{ section.id }}-ranking"
            class="brand-new-items__grid{% unless show_ranking_slider %} brand-new-items__grid--compact{% endunless %} grid product-grid contains-card contains-card--product{% if settings.card_style == 'standard' %} contains-card--standard{% endif %} grid--{{ ranking_desktop_cols }}-col-desktop{% if section.settings.ranking_collection == blank %} grid--2-col-tablet-down{% else %} grid--{{ ranking_mobile_cols }}-col-tablet-down{% endif %}{% if show_ranking_slider %} slider slider--tablet grid--peek{% endif %}"
            role="list"
            aria-label="{{ 'general.slider.name' | t }}"
            style="--brand-new-items-desktop-cols: {{ ranking_desktop_cols }};"
          >
            {%- for product in section.settings.ranking_collection.products limit: section.settings.products_to_show -%}
              <li
                id="Slide-{{ section.id }}-ranking-{{ forloop.index }}"
                class="grid__item{% if show_ranking_slider %} slider__slide{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}{% if section.settings.ranking %} ranking-item{% endif %}"
                {% if settings.animations_reveal_on_scroll %}
                  data-cascade
                  style="--animation-order: {{ forloop.index }};"
                {% endif %}
              >
                {% if section.settings.ranking %}
                  <span class="rank">{{ forloop.index }}</span>
                {% endif %}
                {% render 'card-product',
                  card_product: product,
                  media_aspect_ratio: section.settings.image_ratio,
                  image_shape: section.settings.image_shape,
                  show_secondary_image: section.settings.show_secondary_image,
                  show_vendor: section.settings.show_vendor,
                  show_rating: section.settings.show_rating,
                  show_quick_add: section.settings.enable_quick_add,
                  max_title_length: section.settings.max_title_length,
                  section_id: section.id
                %}
              </li>
            {%- endfor -%}
          </ul>
          {%- if show_ranking_slider -%}
            <div class="slider-buttons no-js-hidden">
              <button
                type="button"
                class="slider-button slider-button--prev"
                name="previous"
                aria-label="{{ 'general.slider.previous_slide' | t }}"
                aria-controls="Slider-{{ section.id }}-ranking"
              >
                {% render 'icon-caret' %}
              </button>
              <div class="slider-counter caption">
                <span class="slider-counter--current">1</span>
                <span aria-hidden="true"> / </span>
                <span class="visually-hidden">{{ 'general.slider.of' | t }}</span>
                <span class="slider-counter--total">{{ ranking_products_to_display }}</span>
              </div>
              <button
                type="button"
                class="slider-button slider-button--next"
                name="next"
                aria-label="{{ 'general.slider.next_slide' | t }}"
                aria-controls="Slider-{{ section.id }}-ranking"
              >
                {% render 'icon-caret' %}
              </button>
            </div>
          {%- endif -%}
        </slider-component>
      </div>
      {%- endif -%}

      <div class="cpb-contents-nav tw-my-10">
        <div class="elegance-products-board">
          <p class="tw-text-center tw-text-3xl tw-mb-8 elegance-products-title">PRODUCTS</p>

          <div class="tw-grid md:tw-grid-cols-2 tw-gap-x-8 tw-gap-y-10 tw-mb-8">
            <div>
              <p class="elegance-group-title">POINT MAKEUP</p>
              <div class="tw-grid tw-grid-cols-2 md:tw-grid-cols-4 tw-gap-4">
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=アイ#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_eye.png' | file_url }}" alt="EYE" width="280" height="220">
                  <p class="elegance-item-label">EYE</p>
                </a>
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=リップ#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_lip.png' | file_url }}" alt="LIP" width="280" height="220">
                  <p class="elegance-item-label">LIP</p>
                </a>
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=フェイスカラー#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_face-color.png' | file_url }}" alt="FACE COLOR" width="280" height="220">
                  <p class="elegance-item-label">FACE COLOR</p>
                </a>
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=ネイル#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_nail.png' | file_url }}" alt="NAIL" width="280" height="220">
                  <p class="elegance-item-label">NAIL</p>
                </a>
              </div>
            </div>

            <div>
              <p class="elegance-group-title">BASE MAKEUP</p>
              <div class="tw-grid tw-grid-cols-3 tw-gap-4">
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=メイクアップベース#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_makeup-base.png' | file_url }}" alt="MAKEUP BASE" width="280" height="220">
                  <p class="elegance-item-label">MAKEUP BASE</p>
                </a>
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=ファンデーション#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_foundation.png' | file_url }}" alt="FOUNDATION" width="280" height="220">
                  <p class="elegance-item-label">FOUNDATION</p>
                </a>
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=フェイスパウダー#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_face-powder.png' | file_url }}" alt="FACE POWDER" width="280" height="220">
                  <p class="elegance-item-label">FACE POWDER</p>
                </a>
              </div>
            </div>

            <div>
              <p class="elegance-group-title">TOOL</p>
              <div class="tw-grid tw-grid-cols-3 tw-gap-4">
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=リムーバー#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_remover.png' | file_url }}" alt="REMOVER" width="280" height="220">
                  <p class="elegance-item-label">REMOVER</p>
                </a>
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=ブラシ#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_brush.png' | file_url }}" alt="BRUSH" width="280" height="220">
                  <p class="elegance-item-label">BRUSH</p>
                </a>
                <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=スポンジ,パフ#Collection">
                  <img class="elegance-item-image" src="{{ 'elegance_category_puff-sponge.png' | file_url }}" alt="PUFF・SPONGE" width="280" height="220">
                  <p class="elegance-item-label">PUFF・SPONGE</p>
                </a>
              </div>
            </div>

            <div>
              <p class="elegance-group-title">SKINCARE</p>
              <a class="elegance-item-card tw-no-underline tw-text-current" href="{{ routes.collections_url }}/elegance?filter.p.tag=スキンケア#Collection">
                <img class="elegance-item-image" src="{{ 'elegance_category_skincare.png' | file_url }}" alt="SKINCARE" width="280" height="220">
                <p class="elegance-item-label">SKINCARE</p>
              </a>
            </div>
          </div>
        </div>
      </div>

      <div class="brand-counseling-banner">
        <a href="https://www.albion.co.jp/counseling/" target="_blank" rel="noopener" class="brand-counseling-banner__link">
          <img
            class="brand-counseling-banner__image"
            src="{{ 'elegance_counseling_banner.jpg' | file_url }}"
            alt="ONLINE COUNSELING オンラインカウンセリング"
            width="1200"
            height="240"
            loading="lazy"
          >
        </a>
      </div>
    </div>
    <!-- Eleganceブランドトップここまで -->
  {% endif %}
</div>
<!-- Eleganceここまで -->

{% schema %}
{
  "name": "商品グリッドELEGANCE",
  "class": "section",
  "settings": [
    {
      "type": "range",
      "id": "products_per_page",
      "min": 8,
      "max": 24,
      "step": 4,
      "default": 16,
      "label": "t:sections.main-collection-product-grid.settings.products_per_page.label"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 5,
      "step": 1,
      "default": 4,
      "label": "t:sections.main-collection-product-grid.settings.columns_desktop.label"
    },
    {
      "type": "header",
      "content": "t:sections.main-collection-product-grid.settings.header__3.content"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__2.label"
        },
        {
          "value": "square",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__3.label"
        }
      ],
      "default": "adapt",
      "label": "t:sections.main-collection-product-grid.settings.image_ratio.label"
    },
    {
      "type": "select",
      "id": "image_shape",
      "options": [
        {
          "value": "default",
          "label": "t:sections.all.image_shape.options__1.label"
        },
        {
          "value": "arch",
          "label": "t:sections.all.image_shape.options__2.label"
        },
        {
          "value": "blob",
          "label": "t:sections.all.image_shape.options__3.label"
        },
        {
          "value": "chevronleft",
          "label": "t:sections.all.image_shape.options__4.label"
        },
        {
          "value": "chevronright",
          "label": "t:sections.all.image_shape.options__5.label"
        },
        {
          "value": "diamond",
          "label": "t:sections.all.image_shape.options__6.label"
        },
        {
          "value": "parallelogram",
          "label": "t:sections.all.image_shape.options__7.label"
        },
        {
          "value": "round",
          "label": "t:sections.all.image_shape.options__8.label"
        }
      ],
      "default": "default",
      "label": "t:sections.all.image_shape.label",
      "info": "t:sections.all.image_shape.info"
    },
    {
      "type": "checkbox",
      "id": "show_secondary_image",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_secondary_image.label"
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_vendor.label"
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_rating.label",
      "info": "t:sections.main-collection-product-grid.settings.show_rating.info"
    },
    {
      "type": "checkbox",
      "id": "enable_quick_add",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.enable_quick_buy.label"
    },
    {
      "type": "number",
      "id": "max_title_length",
      "default": 28,
      "label": "t:sections.main-collection-product-grid.settings.max_title_length.label",
      "info": "指定した最大文字数には省略記号（…）1文字も含まれます。したがって実際の本文はその分短く表示されます。さらにHTMLエスケープ（例: & → &amp;）の影響で、見た目の文字数が1〜数文字少なくなる場合があります。"
    },
    {
      "type": "header",
      "content": "t:sections.main-collection-product-grid.settings.header__1.content"
    },
    {
      "type": "checkbox",
      "id": "enable_filtering",
      "default": true,
      "label": "t:sections.main-collection-product-grid.settings.enable_filtering.label",
      "info": "t:sections.main-collection-product-grid.settings.enable_filtering.info"
    },
    {
      "type": "select",
      "id": "filter_type",
      "options": [
        {
          "value": "horizontal",
          "label": "t:sections.main-collection-product-grid.settings.filter_type.options__1.label"
        },
        {
          "value": "vertical",
          "label": "t:sections.main-collection-product-grid.settings.filter_type.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.main-collection-product-grid.settings.filter_type.options__3.label"
        }
      ],
      "default": "horizontal",
      "label": "t:sections.main-collection-product-grid.settings.filter_type.label",
      "info": "t:sections.main-collection-product-grid.settings.enable_filtering.info"
    },
    {
      "type": "checkbox",
      "id": "enable_sorting",
      "default": true,
      "label": "t:sections.main-collection-product-grid.settings.enable_sorting.label"
    },
    {
      "type": "header",
      "content": "t:sections.main-collection-product-grid.settings.header_mobile.content"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "default": "2",
      "label": "t:sections.main-collection-product-grid.settings.columns_mobile.label",
      "options": [
        {
          "value": "1",
          "label": "t:sections.main-collection-product-grid.settings.columns_mobile.options__1.label"
        },
        {
          "value": "2",
          "label": "t:sections.main-collection-product-grid.settings.columns_mobile.options__2.label"
        }
      ]
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    },
    {
      "type": "header",
      "content": "ブランドトップ（NEW ARRIVAL / RANKING）"
    },
    {
      "type": "collection",
      "id": "new_arrival_collection",
      "label": "NEW ARRIVAL に表示するコレクション"
    },
    {
      "type": "collection",
      "id": "ranking_collection",
      "label": "RANKING に表示するコレクション"
    },
    {
      "type": "checkbox",
      "id": "ranking",
      "default": true,
      "label": "RANKING に順位番号を表示する"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 2,
      "max": 25,
      "step": 1,
      "default": 4,
      "label": "t:sections.featured-collection.settings.products_to_show.label"
    },
    {
      "type": "checkbox",
      "id": "enable_desktop_slider",
      "label": "t:sections.featured-collection.settings.enable_desktop_slider.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "swipe_on_mobile",
      "default": true,
      "label": "t:sections.featured-collection.settings.swipe_on_mobile.label"
    }
  ]
}
{% endschema %}
